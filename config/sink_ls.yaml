sink:
  ros__parameters:
    file_output: "/home/ros2/tesi/topic_tester/latency_ls_inter"
    msg_type: 0
    # 0 = laserscan
    # 1 = pointcloud
    rolling_window_size: 20
    msgs_samples: 200       # how many samples to record before exiting
    long_mode: true
    long_mode_total_groups: 20
    long_mode_period: 1800 # [seconds] between two group
    # n.b. every group will record ${msgs_samples} messages


    